/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn how to create Gradle builds at https://guides.gradle.org/creating-new-gradle-builds/
 */
 
apply plugin: 'java'
//apply from: 'bin/build/repo.gradle'
def VERSIONS ='SNAPSHORT'
def baseDir = rootProject.projectDir
//def lib= "$rootProject.projectDir\\bin\\lib"

ext{
//    def lib= "$rootProject.projectDir\\bin\\lib"
   println "${baseDir}"
   println "${baseDir}\\gradle-plugin\\cxbuild.gradle"
}

allprojects{
    apply from :"${baseDir}\\gradle-plugin\\cxbuild.gradle"

}

subprojects{
 group = 'saar.monitor'
 version = "${VERSIONS}"
}

jar {
   
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
  manifest.attributes provider: 'gradle' 
}
dependencies {
   // runtime fileTree(dir: "$rootProject.projectDir\\bin\\lib", include: '*.jar')
}

